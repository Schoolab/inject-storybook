# inject-storybook

Run local server
```npm run storybook```

Build before deploying
```npm run build-storybook```

Deploying
```git add .```
```git commit -m "your message here"```
```git push origin master```
